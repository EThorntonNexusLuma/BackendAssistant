# --------- Railway Environment Variables (copy into Railway -> Variables) ---------
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:YOUR_PORT/railway

# Google OAuth (from Google Cloud Console)
GOOGLE_CLIENT_ID=600030275595-865ij8ckjo80li2r4bmf7amoja437dk3.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=https://nodejs-production-2b6d.up.railway.app/api/oauth/google/callback

# Frontend and Backend URLs for CSP and redirects
DASHBOARD_URL=https://nexus-luma-ai-assist-3hps.bolt.host
PUBLIC_API_BASE=https://nodejs-production-2b6d.up.railway.app

# Allow these frontends to call the API (comma-separated, no spaces)
CORS_ORIGINS=https://nexus-luma-ai-assist-3hps.bolt.host,https://app.nexusluma.com,http://localhost:5173

# Optional session secret (if you add sessions later)
JWT_SECRET=super-long-random-string
